dataset:
  config: configs/dataset.synthetic.yaml
  root: data/synthetic_soft_robot_vla
  episode_index: 0

policy:
  policy_type: smolvla
  checkpoint: outputs/smolvla_expert_smoke/checkpoints/last/pretrained_model
  device: cuda

inference:
  dry_run: true
  max_frames: 40
  output_dir: outputs/offline_inference_smolvla
