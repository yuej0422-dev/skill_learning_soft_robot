data:
  zip_path: /home/yuej/skill_learning_soft_robot/vision_to_state/data/VLA_PROJECT1.zip
  manifest_path: /home/yuej/skill_learning_soft_robot/vision_to_state/runs/camera2_manifest.json
  camera: camera2
  image_column: 3
  depth_column: 4
  state_columns: last12
  camera_dir: depth_cam2
  seq_len: 4
  crop_left_frac: 0.25
  crop_top_frac: 0.10
  image_size: 192
  depth_clip_mm: 1500.0
  split_seed: 7
  train_ratio: 0.80
  val_ratio: 0.10
  min_rows: 16
  min_tcp_range_norm: 1.0e-6
  linearity_window: 12
  linearity_residual_tol: 1.0e-6
  linearity_min_disp: 0.01
  discard_record_if_linear_fraction_ge: 0.20

train:
  run_dir: /home/yuej/skill_learning_soft_robot/vision_to_state/runs/camera2_rgbd_state
  epochs: 30
  batch_size: 24
  lr: 0.0003
  weight_decay: 0.0001
  num_workers: 2
  device: auto
  grad_clip_norm: 1.0
  amp: true
  save_every_epochs: 5
  max_records: null
  max_samples: null

loss:
  pose_weight: 1.0
  velocity_weight: 0.20

model:
  in_channels: 4
  frame_embed_dim: 256
  temporal_hidden_dim: 256
  dropout: 0.15
