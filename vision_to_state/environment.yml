name: soft_robot_state
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
      - numpy
      - pillow
      - tqdm
      - pyyaml
      - matplotlib
      - pyarrow
